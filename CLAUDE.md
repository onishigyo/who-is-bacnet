# CLAUDE.md

このリポジトリで作業する Claude Code / claude.ai への指針。

## これは何か

BACnet（ビル設備の制御プロトコル）が、**同じネットワークにいるだけで認証なしに操作できてしまう**という事実を、ネットワーク図の上で体験させ、その解決策である BACnet/SC までを理解させる、ブラウザ完結の学習教材。

**設計思想**: 危険性から入らない。まず「BACnet とは何か」を理解・共感させ、便利さを見せてから、その足元にある無認証の落とし穴を見せる（学習順序 = ①BACnet とは → ②BACnet/IP とは → ③便利な側面 → ④危険性）。全ステップで1つのネットワーク図を育て、最後にそこへ攻撃者が現れる（詳細は [docs/DESIGN.md](docs/DESIGN.md)）。

**やらないこと**: 実際の BACnet 通信は発生させない。UI は「それっぽく動く」再現に留め、バックエンドを持たない。「本物である」ことの担保は、実験で取得した Wireshark キャプチャを答え合わせとして提示することで果たす（公開型でバックエンドを持つのはインフラ・セキュリティ上の負荷が過大なため）。

**制作者の前提**: 制作者はこの分野の専門家ではない。壁打ちで学んだ内容を教材化したものであり、内容は誤りを含みうる。この前提を README に明示し、「専門家の完璧な教材」ではなく「実務者の正直な学習記録」として公開する。

## 正確性の原則（このプロジェクト最重要）

内容が間違ったまま公開されることが、このプロジェクト最大のリスク。以下を厳守する。

- **確立した事実と、推測・理解を明確に区別する。** 規格として確立していること（Who-Is/I-Am の挙動、BACnet/IP が無認証であること、BACnet/SC の証明書認証など）は自信を持って書いてよい。それ以外（特定製品の対応状況、組織の事情、制作者の解釈）は「これは理解であり要検証」と明示するか、書かない。
- **推測で事実を埋めない。** 確認できないことを、もっともらしく断定してはいけない。分からないことは「分からない／未確認」と書く。
- **一次情報を優先する。** 断定的な記述の裏付けには、規格（ANSI/ASHRAE 135）・公式技術文書・実機仕様など一次情報を当てる。裏が取れないものは教材本文に断定として載せない。
- **特定の製品・組織・個人について、断定的・否定的な記述をしない。** 事実確認できた範囲に留め、価値判断を加えない。
- コンテンツ上、確立事実と要検証事項が視覚的に区別できる仕組み（バッジ・注記など）を設けることが望ましい。

## コマンド

パッケージマネージャは **pnpm**。

```bash
pnpm dev           # Vite dev サーバ
pnpm build         # 型チェック → Vite バンドル
pnpm lint          # ESLint
pnpm format        # Prettier で整形
pnpm format:check  # 整形チェック（CI と同じ）
pnpm test          # Vitest 一回実行
pnpm preview       # ビルド結果のプレビュー
```

テストは **Vitest**。純粋ロジック（ステップ状態遷移・会話データ整形など）を対象に置く。

## 開発ワークフロー

- 作業は必ず **feature ブランチ**。`main` へ直接コミットしない。1 Issue = 1 ブランチ。
- 変更は **PR 経由**。マージは merge commit、マージ後はブランチ削除。
- マージ後は `main` を fetch + fast-forward で同期してから次の作業へ。
- 各 PR で `pnpm lint` / `pnpm format:check` / `pnpm build` / `pnpm test` を必ず通す（CI が enforce）。
- UI に影響する変更は、実際にブラウザで「学習の流れとして成立しているか」を確認する。初学者が置いていかれないか、危険性が自分ごととして伝わるかを見る。

## アーキテクチャ

React 19 + TypeScript + Vite。図の描画は **React Flow（@xyflow/react）**。静的サイトとして配布（バックエンドなし）。

### レイヤー（新規コードはその責務の層へ置く）

| 層                           | 責務                                                                                                                                |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| ドメイン型                   | ステップ・ノード・会話メッセージなどの型定義だけ                                                                                    |
| **純粋ロジック**             | ステップ状態遷移、会話データの整形・検証、図の状態計算。React も DOM も触らない。**ここがテストの主対象**                           |
| コンテンツデータ             | 各ステップの解説文・会話（意訳＋実コマンドの二層）・対応する Wireshark 素材の参照。データとして分離し、ロジック・描画から独立させる |
| 描画（React コンポーネント） | React Flow のノード/エッジ描画、パケットアニメーション、イベント配線のみ。判定・遷移はロジック層に委譲                              |

原則:

- **状態遷移・判定・整形を React コンポーネントに書かない。** 純粋関数にしてテストを付け、コンポーネントからは呼ぶだけ。
- **コンテンツ（解説文・会話・素材参照）を、コードにハードコードせずデータとして分離する。** 文言修正がロジック変更にならないようにする。
- 会話は「意訳（初学者向け）」と「実コマンド/プロトコル用語（技術者向け）」の二層で持つ。両者は同じ通信を指し、ステップ3（正常運用）とステップ4（攻撃）で話し手だけが変わる、という対比構造をデータ上も保つ。

### 方向性に迷ったら

[docs/DESIGN.md](docs/DESIGN.md) の中心メッセージ（BACnet/IP は同じネットワークにいるだけで無認証で操作できる、その体感と SC という解決策の理解）と、方向B（1つの世界を育てて最後に壊す）に立ち返る。

## 機微情報・公開上の配慮（Public リポジトリ）

このリポジトリは Public。以下を厳守する。

- **秘密鍵・証明書をコミットしない。** `*.key` / `*.pem` / `certs/` を `.gitignore` で除外済み。実験で作ったオレオレ証明書の鍵も含め、git 履歴にも残さない。
- **実際に他者を攻撃可能なスクリプトを、このリポジトリに含めない。** 学習アプリ本体はブラウザ内完結・実通信なしで安全。実験に使った攻撃側スクリプト（Kali 側の Who-Is/Read/Write を実行するコード等）は、このリポジトリの公開範囲に含めない。
- **Wireshark キャプチャは、公開して問題ない情報だけを含むことを確認してから置く。** 実験は閉域網だが、意図しない通信・情報が写っていないか確認する。教材にはスクリーンショット（Info 欄が読める状態）を用いる。
- **README に「防御を学ぶための教材であり、無許可のシステムへの攻撃を推奨しない」旨を明示する。**
- Issue / PR 本文にも、鍵・実環境の機微な情報を書かない。

## 制作の順序

一度に全部作らず、方向性のブレを避ける。

1. **IP 編（ステップ1〜4）を完成させる。** 最も素材が揃い、制作者の理解も最も深い。ここでネットワーク図・会話アニメ・Wireshark 答え合わせの「型」を作る。
2. **SC 編を追加。** IP 編の仕組みを応用。証明書による参加可否、IP との Before/After 対比。
3. **変換器（ルーター）編を検討。** 未実験のため、まず解説ベース。断定を避け「要検証」を明示する。

## このドキュメントの原則

揮発的な実測値（ファイル行数・テスト件数・コンテンツの細かい文言など）を手書きしない（必ず陳腐化する）。役割・責務・原則・不変の構造だけを書く。数値やコンテンツで縛りたいものは prose ではなくデータ・テスト側に置く。
